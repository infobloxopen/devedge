# Values for the {{ .TitleName }} uFE static-bundle chart (base scaffold).
image:
  repository: {{ .AppID }}-ufe
  tag: latest
  pullPolicy: IfNotPresent
replicaCount: 1
service:
  type: ClusterIP
  port: 80
